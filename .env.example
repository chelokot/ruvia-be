# --- Firebase Admin ---
# Set Firebase service account credentials for Admin SDK
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
# Use literal \n for newlines in the private key value
FIREBASE_PRIVATE_KEY=

# --- FAL AI ---
FAL_KEY=
FAL_MODEL=fal-ai/flux/schnell

# --- Google Play Billing ---
# Must match your Android applicationId/package in app.json
ANDROID_PACKAGE_NAME=
# Absolute path to service account JSON with Android Publisher API access
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/play-service-account.json
